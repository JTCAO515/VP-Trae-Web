{
  "name": "@visepanda/openapi",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm run lint && node ./scripts/build.mjs",
    "lint": "redocly lint openapi.yaml",
    "test": "pnpm run lint"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.5"
  }
}
